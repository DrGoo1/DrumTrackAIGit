# DrumTracKAI Enhanced FastAPI Backend Requirements
# Core FastAPI Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Request/Response Validation
pydantic>=2.5.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0

# WebSocket Support
websockets>=12.0

# Database Support
sqlalchemy>=2.0.0
alembic>=1.12.0

# Audio Processing (Core)
numpy>=1.21.0
scipy>=1.7.0
librosa>=0.9.0
soundfile>=0.10.0

# Machine Learning (Optional for advanced features)
torch>=2.0.0
torchaudio>=2.0.0

# Data Processing
pandas>=1.3.0

# Network/API
requests>=2.25.0
urllib3>=1.26.0
aiohttp>=3.9.0
aiofiles>=23.2.0

# Utilities
pathlib2>=2.3.0
python-dotenv>=0.19.0

# Development/Testing
pytest>=6.2.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Additional Audio Libraries
pydub>=0.25.0
aubio>=0.4.9
mutagen>=1.47.0

# Logging and monitoring
structlog>=23.2.0

# Background tasks
celery>=5.3.0  # Optional for production scaling
redis>=5.0.0   # Optional for production scaling

# Development tools
black>=23.0.0
isort>=5.12.0
mypy>=1.7.0

# Production server
gunicorn>=21.2.0

# Optional: Advanced audio analysis
# essentia>=2.1b6.dev1034    # Uncomment for advanced audio analysis
# madmom>=0.16.1             # Uncomment for music information retrieval

# Installation Instructions:
# 1. Basic installation: pip install fastapi uvicorn python-multipart
# 2. Full installation: pip install -r requirements.txt
# 3. For development: pip install -r requirements.txt black isort mypy
# 4. For production: Add environment variables for JWT_SECRET_KEY, MVSEP_API_KEY

# Environment Variables Required:
# - JWT_SECRET_KEY: Secret key for JWT token signing
# - MVSEP_API_KEY: API key for MVSep service (get from mvsep.com)
# - DATABASE_URL: Database connection string (optional, defaults to SQLite)

# Optional GPU Support:
# - Install CUDA Toolkit from https://developer.nvidia.com/cuda-toolkit
# - Uncomment: # cupy-cuda11x>=10.0.0  # For CUDA 11.x support

# Notes:
# - Remove aiohttp if you encounter SSL errors (FastAPI doesn't require it)
# - All dependencies are optional except fastapi, uvicorn, and python-multipart
# - Audio processing will work in mock mode if librosa/soundfile not available
# - Database will use in-memory storage if SQLAlchemy not available